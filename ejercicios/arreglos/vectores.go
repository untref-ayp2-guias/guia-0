package arreglos

// Escribir una función que reciba dos arreglos (que representan vectores) del mismo tamaño y devuelvan la suma y el producto escalar de los vectores
// Pre: len(vector1) == len(vector2)
// Post: Devuelve la suma y el producto escalar de los vectores.

func SumaProductoEscalar(vector1, vector2 []int) (int, int) {
	return 0, 0
}
